<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.binht.testvocabulary">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TestTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TracNghiem" />
        <activity android:name=".Compare" />
        <activity android:name=".Vocabularies" />
        <activity android:name=".speak_word" />
        <activity android:name=".Listen" />
        <activity android:name=".Youtube_Player" />
        <activity android:name=".TestCompare" />
        <activity android:name=".WordTest"></activity>
    </application>

</manifest>